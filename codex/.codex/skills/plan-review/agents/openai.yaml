interface:
  display_name: "Plan Review"
  short_description: "Resolve @me comments in a plan file, one round"
  default_prompt: "Use $plan-review to resolve the @me comments in the given plans/plan-<slug>.md and rewrite it in place."
